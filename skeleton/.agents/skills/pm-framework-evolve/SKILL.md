---
name: pm-framework-evolve
description: PM 框架自迭代 — 框架百科查询、安全迭代、迭代记录、知识库自更新。PM 需要修改框架核心文件或查询框架知识时激活。
---

# PM 框架自迭代

## 适用场景

PM 角色在以下场景**必须**激活本 Skill：

1. **查询框架知识**：需要了解框架架构、功能、文件职责时 → 读取 `references/`
2. **修改框架文件**：需要修改 `.agents/`、`.trellis/spec/`、`.trellis/config/` 时 → 执行安全迭代流程
3. **查看迭代历史**：需要了解框架的修改记录时 → 读取 `changelog/`

> **实时性内容**（如最新 Issues、Release 历史）应由 AI 实时访问 GitHub 仓库获取，不写死在文件中。

## 管控范围

以下 **框架核心目录** 的修改必须通过本 Skill 执行：

- `.agents/rules/` — 规则文件
- `.agents/workflows/` — 角色入口
- `.agents/skills/` — 能力模块
- `.trellis/spec/` — 项目规范（也可通过 `common-spec-update` Skill 处理）
- `.trellis/config/` — 框架配置

> `.trellis/tasks/` 和 `.trellis/workspace/` 属于运行时数据，不受本 Skill 管控。

---

## 安全迭代流程

### Step 0：知识加载（前置）

在开始任何框架变更前：

1. 读取 `references/` 理解当前框架结构和设计理念
2. 读取 `changelog/index.md` 了解已有的迭代历史
3. 基于知识判断本次变更的合理性

### Step 1：变更清单

列出所有将要修改的文件及变更类型：

```markdown
## 框架变更清单

| 文件 | 变更类型 | 变更摘要 |
|------|----------|----------|
| .agents/rules/xxx.md | 修改 | 增加 xxx 约束 |
| .agents/skills/yyy/SKILL.md | 新建 | yyy 能力模块 |
```

### Step 2：影响范围分析

```markdown
## 影响范围报告

### 直接影响
- 变更文件数：{n} 个
- 变更类型：新建 / 修改 / 删除

### 间接影响
- 关联角色：{受影响的 PM / Worker 角色行为变更}
- 关联 Skills：{依赖受影响文件的其他 Skills}
- 约束层级：{是否影响约束优先级体系}

### 风险等级
- 低：仅新增能力，不影响现有行为
- 中：修改已有文件，可能影响角色行为
- 高：修改 Rules 或核心 Workflow，影响所有会话
```

### Step 3：用户确认门

将变更清单和影响范围报告呈现给用户，**等待明确确认后才执行写入**。

**用户确认后**：
1. 逐步执行变更
2. 每步完成后验证文件完整性

**用户拒绝**：
- 放弃全部变更，保持原文件不动
- 记录拒绝原因到 journal

### Step 4：执行变更 + 变更后验证

- 执行文件修改
- 检查修改后的文件是否格式完整、语法正确
- 如涉及 MCP Server 代码，确保 `npm run build` 通过

### Step 5：记录迭代（后置）

1. 按 `resources/changelog-entry-template.md` 模板生成迭代记录
2. 保存到 `changelog/YYYY-MM-DD_{slug}.md`
3. 更新 `changelog/index.md` 索引
4. 通过 `journal_append()` 记录变更日志

### Step 6：知识库自更新（后置）

迭代完成后，检查 `references/` 中是否有文件被本次变更影响：

- 新增了 Skill → 更新 `features.md`
- 新增了 Rule → 更新 `features.md`
- 修改了目录结构 → 更新 `directory-map.md`
- 修改了安装功能 → 更新 `installation.md`

> **自更新边界**：只更新事实性描述，不改变知识库的结构和格式。

---

## 跨项目复用

将本项目的框架修改应用到其他使用 easyAI 的项目：

| 方式 | 说明 | 适用场景 |
|------|------|----------|
| 手动复制 | 根据 `changelog/` 记录，在另一个项目中手动应用 | 少量修改 |
| Git Patch | changelog 中的 diff 可以作为 `git apply` 的输入 | 精确复制代码变更 |
| 整体复制 | 复制整个 `.agents/` 和 `.trellis/config/` + `.trellis/spec/` | 全新项目初始化 |

---

## 迭代分类标签

| 标签 | 定义 | 示例 |
|------|------|------|
| `新增` | 添加框架原本没有的全新功能 | 新增一个 Skill、新增一个 MCP Tool |
| `优化` | 改善现有功能，不改变功能边界 | 优化 Skill 的流程步骤、改善错误提示 |
| `修复` | 修正现有功能的 Bug 或文档错误 | 修复 tool 参数校验遗漏 |
| `修改` | 调整现有功能的行为或配置，在设计走廊内 | 修改约束层级顺序、修改默认配置值 |
| `魔改` | **超出设计意图**的深度改造 | 重写角色系统、替换 MCP 传输协议 |
| `实验性` | 试探性修改，可能会被撤销 | 试验新的上下文管理策略 |

> **修改 vs 魔改判断标准**：问"这个修改是否改变了框架对其他组件的**契约**？" 不改变契约 = 修改，改变契约 = 魔改。

---

## PATEOAS 导航

```markdown
### 下一步行动

1. [ ] 如查询知识 → 读取 `references/` 中对应文档回答用户
2. [ ] 如框架变更 → 走 Step 0-6 完整流程
3. [ ] 变更完成后 → 运行 `npm run build` 验证（如涉及代码）
4. [ ] 使用 `/pm` 或 `/worker {task_id}` 继续后续任务

### 状态快照

- 当前阶段：FRAMEWORK_EVOLVE
- 变更状态：{知识查询 / 已应用 / 已拒绝}
- 变更文件数：{n} 个
- 风险等级：{低/中/高}
- 迭代记录：{已生成/不适用}
- 知识库自更新：{已完成/不适用}
```

## 硬约束

- **必须经用户确认门**：禁止绕过确认直接修改框架文件
- **必须留 journal 记录**：每次框架变更（成功或拒绝）都必须记录
- **不可批量盲改**：每个文件的变更理由必须明确，禁止"顺便改一下"
- **遇到不确定的框架修改决策时，应暂停并与用户沟通**
