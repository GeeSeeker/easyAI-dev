# 外部 CLI 上下文：执行模式 (Execute Mode)

> 本模板由 `common-cli-dispatch` Skill 组装 Prompt 时加载。
> **场景**：Worker 将边界明确、难度适中的编码任务直接委派给外部 CLI 执行。
> **目的**：通过高密度的约束与上下文，获得精准可直接使用的代码输出。

---

## 模式守卫 (Guardrails)

当前会话目标是按照预定的计划和「极强」的约束条件，生成代码或修改文本。
已屏蔽无关的系统全局讨论与长期架构发散回合，确保你将百分百的注意力投入到本次小粒度修改中。

## 强执行约束集 (Constraints)

⚠️ 请将其视为最高优先级的契约，必须 100% 严格遵守。
{constraints}

## 本次执行计划 (Plan)

{plan}

## 目标文件与相关上下文 (Target Content)

{target_content}

## 需遵循规范 (Specifications)

{specifications}

## 执行指示 (Instructions)

请立刻实施以上计划并交付目标产物。你需要遵循：

1. 聚焦：仅输出与本次小粒度目标相关的修改代码。
2. 精确：无需额外的礼貌互动，直接给出现成可用的变更。
3. 完整：若提供代码替换，请确保上下文匹配，可以零阻力平替。
