# 用户自定义目录映射
#
# 用途：声明项目中自定义目录的用途，AI 据此定位文件
# 格式：<路径>  # <用途说明>
#
# 示例：
#   assets/        # 静态资源（图片、图标、字体）
#   scripts/       # 自定义脚本（构建、部署、数据迁移）
#   data/          # 数据文件（CSV、JSON 种子数据）
#   lib/           # 第三方库或本地封装
#   config/        # 应用配置文件
#   public/        # 公开访问的静态文件
#
# 规则：
#   - 每行一个目录，用 # 注释说明用途
#   - 路径相对于项目根目录
#   - 空行和纯注释行会被忽略
#   - 框架目录（.agents/ .trellis/ .docs/）无需声明
#
# 当 AI 需要定位文件时，会读取此文件了解你的目录结构。
# 你可以随时编辑此文件，添加新的目录映射。
